# ── Server ────────────────────────────────────────────────────
HOST=0.0.0.0
PORT=3000

# ── Database (PostgreSQL) ─────────────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=syslog_collector_ai
DB_USER=syslog_ai
DB_PASSWORD=change_me_in_production

# ── Security ──────────────────────────────────────────────────
# Initial admin API key (generated on first run if not set; printed to stdout)
# ADMIN_API_KEY=

# ── Redaction ─────────────────────────────────────────────────
# Set to true to strip secrets/passwords from log content before storage
REDACTION_ENABLED=false
# Comma-separated additional regex patterns (optional; built-in set always applies when enabled)
# REDACTION_PATTERNS=

# ── LLM (Phase 2+) ───────────────────────────────────────────
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini

# ── Alerting tokens (Phase 5+) ────────────────────────────────
# PUSHOVER_TOKEN=
# PUSHOVER_USER_KEY=
# NTFY_BASE_URL=https://ntfy.sh
# NTFY_TOPIC=
# GOTIFY_BASE_URL=
# GOTIFY_APP_TOKEN=
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_CHAT_ID=

# ── Timezone (for application logs) ──────────────────────────
TZ=Europe/Chisinau
